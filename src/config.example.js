/**
 * Example configuration file.
 * Copy this to config.js and adapt it.
 * 
 * Note: src/config.js should be versioned. 
 * If you want local overrides, use src/config.local.js (which is ignored).
 */

// URL of the Google Apps Script Web App (this is NOT a secret)
export const API_URL = 'https://script.google.com/macros/s/XXXX/exec';

// Set to true to use local mock data instead of calling the API
export const USE_MOCK = true;
